run:
  deadline: 2m

linters:
  disable-all: true
  enable:
    - gofmt
    - gosimple
    - gocyclo
    - misspell
    - govet

linters-settings:
  goimports:
local-prefixes: github.com/kubernetes-incubator/descheduler
